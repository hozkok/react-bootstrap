import React from 'react';
import ReactDOM from 'react-dom';

const appStyle = {
  fontFamily: 'Monospace'
}

const App = (props) => (
  <div className="app" style={appStyle}>
    From the coast of roads...
  </div>
);

export default App;
